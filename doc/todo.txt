Сделать новый KafkaExtract, который зависит от числа партиций в топике - читал все партиции, пока не наберет строк в размере батча.
Сделать преобразование типов sql -> avro и avro -> sql для этого нужно создать правила преобразования типов.
 --проблема, при хранении типов в каждом EtlRecord - мы тратим доп. память на мапу с типами, при этом на каждую строку у нас мапа с типами, т.к. разные типы могут быть в sql_variant
    -- реализовать type-conversion.md

Изменить - пользователь указывает полность DB.schema.tableName:
public String generateSelectQuery(String tableName, DataSource dataSource) {
        // 1. Парсить schema.table
        String schema = "dbo"; // default
        String table = tableName;
проверить, где еще в пользовательских данных указана схема dbo или конкретная БД, пользователь должен сам указывать схему и БД в поле с таблицей.


в @doc\cross-database-support-plan.md описаны текущие проблемы. в @doc\implementation-guide-cross-database.md описана реализация, которая исправляет эти проблемы. Реализуй исправление и допиши документацию в @doc\type-conversion.md и @doc\type-conversion-requirements.md
